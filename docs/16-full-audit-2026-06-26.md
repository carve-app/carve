# Carve Full Functionality and Architecture Audit — 2026-06-26

## Executive result

No reproducible P0 issue was found. The audit found and fixed multiple P1
durability, deployment, security, and test-integrity defects. The most important
functional change is retry-safe review: first-party clients now create a stable
event UUID, the database enforces per-user uniqueness, and a replay returns the
original stored scheduling result without scheduling the card twice. Offline
storage failure is visible and blocks the review instead of silently losing it.

The repository now has one frozen pnpm workspace, blocking lint/type/build/test/
audit gates, an explicit Cloudflare-compatible static build, exact route/OpenAPI
parity, authenticated contract fuzz configuration, isolated real-stack proof,
real Chrome and Firefox package execution, a four-project web/mobile browser
matrix, and honest flow documentation.

The baseline is materially stronger but not “fully covered.” Aggregate coverage
of several large legacy Go handler packages remains 30–57%, a diagnostic broad
web mutation run scored 9.43%, Safari has no native wrapper, and some end-to-end
UI mutations/import round trips remain partial. Those are recorded below rather
than being converted into synthetic passes.

## Scope and method

The audit covered the repository, migrations, Docker Compose and phase-one
deployment configuration, Go API/media services, Python NLP service, Rust/WASM,
SvelteKit PWA, Chrome/Firefox/Safari extension bundles, mock and real-stack E2E,
CI/scheduled workflows, OpenAPI, performance scripts, and provider canaries.
Production was not modified. Real-stack tests used isolated ports, databases,
volumes, Mailpit, and non-destructive provider calls.

Statuses are `fixed`, `partial`, `open`, or `failed`. A fix is only marked
`fixed` when a regression check exists. Live-provider results use only
`passed`, `failed`, or `inconclusive`.

## Findings ledger

| ID | Severity | Reproduction evidence and impact | Fix and regression proof | Status |
|---|---|---|---|---|
| F-01 | P1 | Three app-level lockfiles and mixed npm/pnpm installs could resolve different dependency graphs; lint had no runnable flat configuration. | Consolidated the root pnpm workspace (including E2E), removed duplicate lockfiles, added flat Svelte/TypeScript ESLint, and made frozen installs, lint, typecheck, tests, builds, and audits blocking. `pnpm lint`, `pnpm typecheck`, and frozen install all pass. | fixed |
| F-02 | P1 | The prior Svelte/Vite/Vitest/static-copy graph included vulnerable or audit-reported ranges. | Upgraded Svelte 5.56.4, SvelteKit 2.68.0, Vite 8.1, Vitest/browser 4.1.9 and static-copy 4.1.1; pinned fixed transitive `undici`, `cookie`, and `qs`. `pnpm audit --audit-level moderate` reports no known vulnerabilities. The requested SvelteKit version above 2.68.0 does not exist as a stable registry release (latest is 2.68.0; only a 3.0 prerelease exists), so the audit did not substitute an unstable prerelease. | fixed at latest stable |
| F-03 | P1 | `adapter-auto` emitted compatibility warnings and did not produce the directory Cloudflare expected, allowing a successful build followed by an empty deployment. | Replaced it with `adapter-static`, SPA fallback, exact `apps/web/build` publishing, and build/deploy assertions for `build/index.html`. Clean build produces the directory without compatibility warnings. | fixed |
| F-04 | P1 | Upgrading `vite-plugin-static-copy` initially nested copied assets, so a nominally successful extension build had no root manifest and was undeployable. | Added `stripBase` rename behavior and CI assertions for manifest, background, content, popup JS, and popup HTML in every browser bundle. Packaged Chrome and Firefox runtime checks pass. | fixed |
| F-05 | P1 | Contract, mutation, performance, and canary paths contained suppression or warning-only behavior; hourly/nightly jobs lived in ordinary CI. Failures could look green or run at the wrong cadence. | Removed gate suppression, split `scheduled-quality.yml`, made each cron select only its intended job, and kept ordinary CI event-only. Workflow YAML parses successfully. | fixed |
| F-06 | P2 | The mock server returned generic successful empty responses for unknown behavior, and the flow inventory cited nonexistent tests. This could turn route drift into a pass. | Unknown mock routes now return 501 and supported routes validate bodies/auth. `verify-flow-inventory.mjs` checks every cited path; 31 evidence files resolve. | fixed |
| F-07 | P1 | Router setup and infinite background loops were coupled to `main`; the global 30-second middleware also terminated NLP cold starts despite their intended longer budget. | Extracted router construction and scheduled-job lifecycle with injected dependencies. Ordinary API requests retain 30 seconds; NLP routes receive 125 seconds and propagate cancellation to upstream requests. Router, timeout, provider-failure, and cancellation tests pass under `go test -race`. | fixed |
| F-08 | P1 | Every API replica could run ingestion, snapshot, and report jobs concurrently; loops were not context-cancellable. Duplicate work and inconsistent reports were possible during scale-out/restarts. | Added a scheduled-jobs interface, context cancellation, shutdown waiting, and PostgreSQL advisory locks around each job. Lock-contention and cancellation tests are in `cmd/api/jobs_test.go`. | fixed |
| F-09 | P1 | Review submissions had no client idempotency key. A lost response followed by retry could schedule a card twice. The first implementation also reconstructed a replay response from database numeric/time types, which did not byte-match the original. | Added optional API `event_id`, migration `015_review_event_idempotency.sql`, unique `(user_id, client_event_id)`, and a stored JSONB response snapshot. First-party web/extension callers always send UUIDs; legacy callers remain compatible. Real-Postgres replay creates one event and returns the original result. | fixed |
| F-10 | P1 | IndexedDB transaction/open failures were swallowed, so an offline review could advance in the UI without durable local or server state. | Persistence APIs reject on open, transaction, and abort errors. The review UI visibly blocks/reports failure. Browser proof queues 50 events, injects a lost response, drains to zero, observes exactly 50 unique server IDs, and no double scheduling. | fixed |
| F-11 | P1 | Registration/reset generated tokens but did not deliver them through an injectable mail boundary; token handling was difficult to test safely. | Added SMTP/no-op/fake mailers, Mailpit configuration, verification/reset delivery, injected clock, and removal of raw production token logging. The isolated stack proves both email journeys through Mailpit. | fixed |
| F-12 | P1 | `/metrics` was publicly readable when exposed and `/health` did not distinguish process liveness from dependency readiness. | Added bearer protection via `METRICS_TOKEN` with constant-time comparison, `/live`, and DB-aware `/ready`; retained `/health` compatibility. Compose/deployment health checks use readiness. Auth and dependency-failure tests pass. | fixed |
| F-13 | P2 | The distroless media image depended on shell-style health assumptions it could not execute. | Added a self-contained `media healthcheck` command to the binary and wired Docker health checks to it. Media race tests and Compose validation pass. | fixed |
| F-14 | P2 | Historical duplicate `010`/`011` migration prefixes made future ordering errors easy and were not validated. Fresh and upgrade paths were not both enforced. | Preserved historical filenames, added strict naming/order/duplicate-prefix validation for new files, staged 001–014 to 015 upgrade, replay, fresh-install, and logical backup/restore checks. Isolated restore preserved one seeded user and all 17 migration records. | fixed |
| F-15 | P2 | Registered API routes and `openapi.yaml` had drifted; discover, reports, grammar, export, and NLP variants were incomplete. | Completed the additive spec and added exact method/path parity against the constructed runtime router. The parity test is part of the Go race suite; authenticated Schemathesis is blocking in CI. | fixed |
| F-16 | P1 | Load checks could count fast 401/400/404 responses as successful performance samples and did not seed valid state. | k6 now registers valid users, creates cards, authenticates requests, checks response semantics, and fails on non-success status codes. Lighthouse assertions are blocking. | fixed |
| F-17 | P2 | “Cross-browser” meant mostly manifest/build checks. Firefox had no packaged runtime proof, Safari status was overstated, and mobile WebKit was absent from CI. | Added real Firefox installation/content-script smoke through `web-ext`, package lint, Chrome runtime E2E, WebKit functional journeys, and mobile WebKit CI. Safari remains explicitly unverified until a native Safari Web Extension wrapper/signing project exists. | partial |
| F-18 | P1 | Mock journeys were being treated as backend integration evidence. | Added `test-real-stack.sh`: isolated Postgres, Redis, NLP, media, MinIO, Mailpit, real Go migrations/API, built web/extension, and Chromium. It proves mail auth flows, ten-language tokenization, card creation, and exact review replay. | fixed |
| F-19 | P1 | The live YouTube canary passed when the overlay container mounted even if it contained no subtitle cue. | Canary now requires a non-empty `#cso-target`, waits 60 seconds, and uses a public caption-oriented default. The hardened 2026-06-26 run correctly failed because YouTube exposed no captions; it did not report a false pass. | fixed (provider result failed) |
| F-20 | P2 | Web unit coverage was minimal. Changed legacy Go packages currently report: `cmd/api` 57.4%, review 40.2%, auth 30.0%, cards 38.6%, NLP proxy package 50.3%; only metrics exceeds 80% at 80.7%. | Added ≥80% line gates for the changed offline/storage durability modules (web 98.03%, extension 100%) and focused Go regression tests. Raising the aggregate legacy handler packages to 80% requires a larger handler decomposition/test program. | partial |
| F-21 | P2 | Stryker’s Vitest plugin was not discovered under pnpm, so mutation CI failed before executing mutants. A diagnostic whole-web scope then scored only 9.43%. | Declared the runner plugin explicitly and disabled unsafe incremental reuse. Blocking changed-risk scopes now pass: web offline 67.74%, extension storage 80.95%, both above 65%. Expanding the threshold to the whole UI/API client remains open. | partial |
| F-22 | P2 | Production provider calls were embedded in handlers, making deterministic failure testing expensive. | Added internal interfaces for mail, NLP HTTP, media upload, TTS synthesis, clocks, and scheduled jobs, plus malformed/timeout/interruption tests. Some report/output/translation orchestration remains coupled. | partial |
| F-23 | P2 | Compose proof runs could collide with a developer’s default ports and deployment services gated only on process health. | Parameterized host ports/project name for the isolated stack and switched API/deployment dependencies to readiness. Root and phase-one Compose files pass `docker compose config`. | fixed |
| F-24 | P2 | The Playwright default matrix ran Chromium-only visual baselines on Firefox and installed Chromium for the iPhone/WebKit project; the full local command therefore failed for infrastructure reasons. | Restricted visual comparison to its maintained Chromium/macOS baseline, kept functional/a11y tests on all engines, and mapped mobile to WebKit explicitly. The matrix now passes 110 tests across four projects; six provider/real-stack cases skip unless enabled. | fixed |
| F-25 | P2 | Onboarding swallowed known-word and starter-deck API failures, advanced to the next step, and later claimed those operations succeeded. | Onboarding now remains on the failing step, exposes the API error in an accessible alert, and permits retry. A Playwright fault-injection journey returns 503 and proves the wizard does not advance. | fixed |
| F-26 | P3 | Card, auth, and UI modules remain large, increasing change blast radius and suppressing attainable package coverage. | Split only the boundaries needed for router/jobs/providers/review persistence in this audit. Further handler/UI decomposition is architectural backlog. | open |
| F-27 | P3 | Native iOS/Android mining, EPUB import, a Safari wrapper, and a real Anki-process integration are not shipped. | Documentation and flow inventory label them unverified/partial; no mock result is used as evidence. | open |

## Verification evidence

| Gate | Result on 2026-06-26 |
|---|---|
| ESLint | passed |
| Svelte/TypeScript type checks | passed, zero diagnostics |
| Web unit/coverage | 10 passed; `offline.ts` 98.03% lines |
| Extension unit/coverage | 144 passed; `storage.ts` 100% lines |
| Targeted JS mutation | passed; web 67.74%, extension 80.95% |
| Go API | all packages passed with race detector |
| Go media | passed with race detector |
| Python NLP | 626 passed; one upstream FastAPI deprecation warning |
| Rust/WASM tokenizer | 6 passed |
| Workspace production builds | passed; static web output and all three browser bundles present |
| Dependency audit | passed; no known vulnerabilities |
| Route/OpenAPI parity | passed |
| Browser functional/a11y/mobile matrix | 110 passed, 6 conditional skips |
| Chromium visual regression | 6 passed |
| Packaged Chromium annotation/mining | passed |
| Packaged Firefox content-script smoke | passed in Firefox stable 152 |
| Offline 50-event browser proof | passed |
| Isolated real stack | passed; one complete Playwright proof journey |
| Fresh/staged migrations and replay | passed |
| PostgreSQL backup/restore | passed; data and 17 migration records preserved |
| Root and deployment Compose render | passed |
| Workflow YAML parsing | passed |

The GitHub-hosted Schemathesis, Linux visual, scheduled provider, performance,
and non-JavaScript mutation jobs are configured as blocking gates but were not
executed by a GitHub runner during this local audit. Their absence is not
reported as a pass.

## Live-provider results

| Provider/flow | Result | Tested environment and reason |
|---|---|---|
| Public YouTube subtitle annotation/mining | failed | Built Chrome extension in anonymous Playwright Chromium on macOS arm64, using the public default video. Overlay mounted, but YouTube reported captions unavailable and produced no non-empty cue within 60 seconds. |
| Google Cloud Translation | inconclusive | `GOOGLE_APPLICATION_CREDENTIALS` and `GOOGLE_CLOUD_PROJECT` were not available in the audit environment. |
| Google Cloud TTS | inconclusive | Service-account credentials were unavailable; no mock result was promoted to live proof. |
| Anthropic contextual explanation | inconclusive | `ANTHROPIC_API_KEY` was unavailable. |
| Netflix, Disney+, Prime Video, Crunchyroll, Viki authenticated playback | inconclusive | No authenticated provider session was in scope. Recorded DOM fixtures passed but are not live-provider evidence. |

## Prioritized remaining work

1. Raise aggregate coverage in review/auth/cards/NLP/API handler packages to 80%
   while splitting transport, domain, and persistence logic; expand mutation
   scope only as those tests become meaningful.
2. Complete full UI journeys for edit/bulk/suspend/bury, undo/leech/stats, and
   malformed/oversized/Unicode import-export round trips against the real stack.
3. Isolate the remaining translation/report/output provider orchestration and
   add restart, contention, and interrupted-upload scenarios at process level.
4. Investigate the current public YouTube captions failure and maintain at
   least two known-caption canary videos so a provider-specific removal is
   distinguishable from an extension regression.
5. Build a native Safari Web Extension wrapper/signing project before claiming
   Safari runtime support; add real AnkiConnect and authenticated streaming
   sessions only in appropriately credentialed, non-production test accounts.
