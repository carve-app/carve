# Deploy runbook

The active runbook is [`phase1.md`](./phase1.md).

The old ECS/Fargate runbook was removed with the larger AWS stack. Phase 1 now
uses Terraform for long-lived infrastructure and Docker Compose scripts for app
releases.
