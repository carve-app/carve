{
  "name": "@carve/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.320",
    "typescript": "^5.8.0",
    "vite": "^5.0.0"
  }
}
