{
  "name": "@carve/extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "BUILD_TARGET=background vite build --watch --mode development",
    "build": "BUILD_TARGET=background vite build && BUILD_TARGET=content vite build && BUILD_TARGET=popup vite build",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.320",
    "typescript": "^5.8.0",
    "vite": "^5.0.0",
    "vite-plugin-static-copy": "^1.0.0"
  }
}
