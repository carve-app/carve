# go-mutesting configuration — invoked from `make test-mutation-api`.
#
# Targets the FSRS, importer (anki_sched.go), output (transcribe.go),
# and metrics packages where pure logic dominates. SQL handlers are
# covered by the L3 integration suite, not mutation testing.
#
# Run:
#   go install github.com/avito-tech/go-mutesting/cmd/go-mutesting@latest
#   go-mutesting --config .mutation.txt --threshold 0.65 \
#                ./internal/fsrs/... \
#                ./internal/importer/... \
#                ./internal/output/... \
#                ./internal/metrics/...
#
# CI parses the resulting mutation score; <65% blocks merge.
exec_timeout = 30
parallel = 4
skip_files = [
  "*_test.go",
  "*/cmd/*",
]
